# Fortran ↔ icon4py `amps` Package Mapping (M1)

Seed of the Fortran↔Python mapping doc for the `amps` port. Ground truth for
this table is the committed M1 task reports in the icon4py worktree
(`/Users/jcanton/projects/icon4py/.worktrees/amps_microphysics/.superpowers/sdd/m1-task-{3..8}-report.md`;
tasks 1–2's reports were not retained in that directory — sourced instead
from `core/constants.py`/`core/thermo.py`'s own module docstrings, which cite
the same fact files) and the fact extracts under
`docs/superpowers/facts/m1/` (F1–F5, see the M1 plan
`docs/superpowers/plans/2026-07-17-amps-port-m1-foundations.md`). Update this
file as later milestones port more of `mod_amps_core.F90`.

M2a (warm-phase: liquid activation + condensation/evaporation + liquid
diagnosis) additions are appended below the M1 table — see "M2a module →
Fortran source map" and the M2a rows added to "Known divergences".

icon4py package root: `model/atmosphere/subgrid_scale_physics/amps/src/icon4py/model/atmosphere/subgrid_scale_physics/amps/`
(paths below are relative to this root unless stated otherwise).

## Module → Fortran source map

| Python module | Fortran source(s) | Fact file | Notes |
|---|---|---|---|
| `core/constants.py` (`AmpsConst`) | `contrib/AMPS/mod_amps_const.F90` (base constants); `contrib/AMPS/acc_amps.F90` (precomputed geometry constants) | F1 §1–§2 (`constants-thermo.md`) | Derived constants (`MR`, `Racp`, `Rdvchiarui`, `M_a`) computed from expressions, not hardcoded. CGS units throughout. |
| `core/thermo.py` — `make_esat_tables`/`esat_lk` | `mod_amps_utility.F90` `QSPARM2` (table build, Murphy–Koop 2005); table accessor semantics | F1 §3a, §3c | Fortran-exact truncate-then-clamp index arithmetic (`I=MAX(1,MIN(int(T)-163,149))`) preserved verbatim (M0 carry-forward #4). |
| `core/thermo.py` — `esat_analytic` | Lowe & Ficke analytic formula | F1 §3b | Alternate to the table accessor; NOT the default path used elsewhere in the port. |
| `core/thermo.py` — `t_from_esat_lk` | reverse-search-from-table routine | F1 §3d | ±5-entry window + full-scan fallback preserved. |
| `core/thermo.py` — thermo coefficient fns (D_v, k_a, d_vis, GTP/MGTP, sig_wa) | `class_Thermo_Var.F90` coefficient routines | F1 §3e+ | One function per Fortran routine. |
| `core/thermo.py` — `cal_til`/`cal_thetail`/`diag_t` equivalents | `class_Thermo_Var.F90` θ_il closure (`cal_til`, `cal_thetail`, `diag_t`) | F1 §5 | T-from-θ_il+condensate-loadings closure, numpy. |
| `core/bin_grid.py` | `binmicrosetup_scale` (liquid + ice bin-boundary construction) | F2 §1 (`bins-config-indexmaps.md`) | Liquid 40/80-bin, ice 10/20/40-bin; `binb[i+1]=a_b*binb[i]+b_b` recurrence, per-bin (not scalar) `a_b`/`b_b`. Fortran "last-loop-iteration overwrites `binbr(nbr+1)`" quirk deliberately preserved (rel=1e-12, not bit-exact, on that one boundary). |
| `config.py` (`AmpsConfig`) | `contrib/AMPS/AMPSTASK.F` (`/AMPS_param/` namelist, read via `read_AMPSTASK`); `PARAM_ATMOS_PHY_MP_AMPS_bin` namelist (scale-side) | F2 §4–§6 | `cloudlab()`/`cloudlab_seeding()` classmethods pin exact cloudlab/restart `run.conf` values. `micexfg` kept both as named booleans/ints and as `micexfg_array()`. |
| `core/index_maps.py` | `contrib/AMPS/par_amps.F90` (9 index-space groups: `LiquidPPV`, `IcePPV`, `AerosolPPV`, `*MassIndex`, `*MassRatioIndex`, `IceAxisIndex`); `contrib/AMPS/maxdims.F90` (6 true `parameter`s) | F2 §2–§3 | `FortranIndex.py_idx` gives 0-based access while preserving 1-based Fortran `.value`. |
| `codegen/convert_luts.py` (offline converter, not packaged) | `RDCETB`/`RDSTTB` readers (collision-efficiency, habit-frequency, diagnostic-map, `stdnorm.dat` files) | F3 (`lut-files.md`) | Parses `AMPS_DATA/*` into `amps_luts_collision.npz` + `amps_luts_misc.npz` (split to satisfy `check-added-large-files`, 500 KB/file default). |
| `core/lookup_tables.py` — `load_luts` | packaged npz loader (`importlib.resources`) | F3 §4 | `AmpsLuts` frozen dataclass mirrors Fortran `sequence` derived types. |
| `core/lookup_tables.py` — `init_osmo_par` equivalent (`_osm_ammsul`/`_osm_sodchl`) | `mod_amps_utility.F90` `init_osmo_par`/`osm_ammsul`/`osm_sodchl` | F3 §5.1 | x-grid for `osm_ammsul` transcribed verbatim from the real Fortran read (`mod_amps_utility.F90:12996`) after Addendum 1 corrected an earlier inferred-uniform-grid guess; `osm_sodchl`'s x-grid likewise verbatim. |
| `core/lookup_tables.py` — `init_normal_lut`/`init_inv_normal_lut` | DCDFLIB `cdfnor`-based normal/inverse-normal CDF LUT generators | F3 §6 | Ported via `scipy.stats.norm.sf`/`.ppf` (F3 explicitly sanctions this as the intended equivalent). |
| `core/lookup_tables.py` — `init_inherent_growth_par` | IGP spline-knot generator | F3 §5 | Verbatim, no gaps; 23 knots. |
| `core/lookup_tables.py` — `breakup_fragment_table_sizes`/`make_breakup_fragment_tables` | `cal_breakfragment` (`mod_amps_lib.F90:1831-2017`) — SIZING ONLY | F3 §5.5 | See "Not ported yet" — fragment-mass/-count physics fill is NOT implemented; `bu_fd`/`bu_tmass` are zero-filled and hard-guarded (`NotImplementedError` unless `allow_placeholder=True`, plus a runtime `is_placeholder` flag on the returned dataclass). |
| `core/state.py` (`LiquidState`/`IceState`/`AerosolState`) | `qrpv`/`qipv`/`qapv` array layouts (`(nprops, nbins, ncat, npoints)`), PPV property axis order per `par_amps.F90` | F2 §2, F4 §1.1 | Uses `core/index_maps.py`'s `LiquidPPV`/`IcePPV`/`AerosolPPV` for the property axis order (1-based Fortran `.value` ↔ axis index). |
| `core/state.py` (`ThermoState`) | `Z_LOOP_01` thermo block (`ptotv, tv, thv, piv, pbv, moist_denv, qvv, thetav, wbv, momv`) | F4 §1.1 | New, package-local `ThermoProp` enum — no Fortran `qXpv` array backs it. `ncat != 1` raises `NotImplementedError` (matches Fortran's own "assumed to be 1" scope). |
| `core/packing.py` — `pack_scale_to_amps` | `Z_LOOP_01`, `scale_atmos_phy_mp_amps.F90:1625-1828` | F4 §1 | `factor_mxr1` mixing-ratio conversion; aerosol-into-drop-mass addition (`rmt_q`, `imt_q`); the ×0.001 unit factor applied to concentration/axis slots only, never mass slots. |
| `core/packing.py` — `unpack_amps_to_scale` | tendency block `scale_atmos_phy_mp_amps.F90:2676-3010`; energy block `:2305-2352` | F4 §2 | Every `RHOQ_t`/`CPtot_t`/`CVtot_t`/`RHOE_t` recipe. `l_gaxis_version=1` fully implemented (incl. v1-only `l_axis_limit` clip-overwrite); `l_gaxis_version ∈ {2,3}` raises `NotImplementedError` in both pack and unpack. |
| `core/packing.py` — `moistthermo_mask` | `moistthermo2_scale`, `scale_atmos_phy_mp_amps.F90:1053-1117` | F4 §3.2 | qc/qr/qi bin partition against `RRLMTB`/`RILMTB=1e-22`; closed-form `qtp`/`thp`. SS3.3's iterative T-refinement is NOT ported (out of task scope, not needed for the closed-form path used). |
| `core/rng.py` | none — counter-based design, graduated from `spikes/spike_e_counter_rng.py` | F5 §6 (`icon4py-m1-conventions.md`) | See "Known divergences" — this is NOT a port of the Fortran LCG. |
| `driver/ref_data.py` | `scale-amps/scripts/amps_dump_reader.py` (stays authoritative for cluster-side `.bin`→npz conversion); record layouts produced by `AMPS_DUMP_micro`/`AMPS_DUMP_sed` (`scale_atmos_phy_mp_amps.F90`, ~lines 5283-5372) | — | Typed port (`MicroRecord`/`SedRecord` dataclasses) of the same binary record layout; endian auto-detect and version checks ported verbatim (version-mismatch now `raise ValueError`, not `assert` — M0 carry-forward #2). |
| `driver/box.py` | box-driver skeleton (no single Fortran routine — orchestration scaffold) | — | `run_box()` raises `NotImplementedError` (M2 wiring target); `case_from_micro_record()` is implemented and reconstructs a `BoxCase` from one `MicroRecord`. |

## M2a module → Fortran source map (warm-phase: liquid activation + condensation/evaporation + liquid diagnosis)

Ground truth: the M2a task reports in the icon4py worktree
(`/Users/jcanton/projects/icon4py/.worktrees/amps_microphysics/.superpowers/sdd/m2a-task-{1..7}-report.md`,
plus `m2a-hardening-report.md` for the Task 7 code-review fixes) and the
M2 fact extracts under `docs/superpowers/facts/m2/`: `micro-tendency-
orchestration.md` ("G1"), `activation.md` ("G2"), `vapor-deposition.md`
("G3"), `sedimentation-terminalvel.md` ("G5"). Scope is WARM (liquid/rain)
only — no ice group exists in `implementations/warm_loop.py`'s
`WarmLoopState`; see "M2a warm-phase: not-yet-ported" below for what is
deliberately excluded.

| Python module | Fortran source(s) | Fact file / task report | Notes |
|---|---|---|---|
| `implementations/warm_loop.py` (`WarmLoopState`, `run_warm_micro_tendency`, `ifc_warm`) | `cal_micro_tendency`/`ifc_cloud_micro` (`class_Cloud_Micro.F90`), WARM-PHASE call subset only | G1, `m2a-task-1-report.md` (skeleton) + `m2a-task-7-report.md` (wiring) | `col_loop1` (`n_step_cl`, dt=`dt_cl`) x `vap_loop` (`n_step_vp`, dt=`dt_vp`) substep structure; refresh preamble `update_mesrc -> diag_t -> update_airgroup [UNMODELED] -> diag_pq` collapsed into one `_refresh_state`; process hooks `_activation`/`_vapor_deposition_liquid` (vap-loop body) and `_repair` (once per col_loop iteration, `phase="collision"`, and once per vap_loop iteration, `phase="vapor"`). Dropped by design: ice-ice aggregation, ice-rain riming, `hydrodyn_breakup(ice)`, `Ice_Nucleation1/2`, `vapor_deposition(ice)`, `mv_ice2liq`, `melting_shedding`; also NOT modeled: `coalescence(rain,rain)`/`hydrodyn_breakup(rain)` (M2b) and `update_group_all`/`cal_dmtend_scale` (no separate tendency-ledger step — each hook returns already-advanced state). |
| `core/liquid_diag.py` (`diag_pq_liquid`, `LiquidDiag`) | `diag_pq`, `phase==1` (liquid) branch (`mod_amps_core.F90:12552`, lines 12786-12835) + 5 contained helpers: `cal_meanmass_vec` (`class_Mass_Bin.F90:1840-1849`), `cal_den_aclen_vec` (`class_Group.F90:11222-11300`), `cal_terminal_vel_vec` phase==1 (`class_Group.F90:8069-8119`), `cal_ventilation_coef_vec` phase==1 + `get_fkn` (`class_Thermo_Var.F90:707-730`), `cal_capacitance_vec` phase==1, `cal_coef_vapdep2_vec` phase==1 | G3 §5, G5; `m2a-task-2-report.md` | `cal_terminal_vel_vec` here is the per-bin (mean-mass-point) Beard-regime fall speed used BY `diag_pq` — NOT the bin-integrated Best-number/Böhm `cal_wterm_vel_v3_vec` the sedimentation driver (`w_terminal_vel`) uses (different function, M2c scope). `eps_map`/`r_n`/`r_n3`/`r_crt`/`r_act`/`cal_surface_temp2_vec` computed internally but deliberately NOT exposed on `LiquidDiag` (outside the task's named field list). Dry-air density (`den_a`) uses the exact mass-conservation identity `den_a = den*(1-qv)` in place of the Fortran's `Make_Thermo_Var`-based `M_a*(P-e)/(R_u*T)` (this port's `ThermoState` doesn't carry actual vapor pressure `e`) — documented exact substitute, not an approximation. |
| `core/activation.py` (`zbrent_act_vec`, `func_liqvap_vec`, `func_icevap_vec`, `func_vec`, `cal_coef_svsteady_init`, `cal_air_temp`, `kohler_*`, `activate_and_advance_vapor`) | `cal_aptact_var8_kc04dep` (`mod_amps_core.F90:5776-9388`, cloudlab's `act_type=1` selection) + contained solvers + `get_critrad_anal`/`get_hazerad_anal` (Köhler critical/haze radius, `class_Mass_Bin.F90`) | G2; `m2a-task-3-report.md` (Part 1: primitives) + `m2a-task-4-report.md` (Part 2: driver) | Masked FIXED-ITERATION numpy (all lanes run the full iteration budget, a boolean "active" mask freezes converged/failed lanes) replaces Fortran's dynamic index-compaction (`mbx`/`Lbx` shrinking every iteration) — mathematically equivalent, not a behavioral shortcut. `func_icevap_vec`'s "partial melt + shed" sub-branch (needs unported `class_Ice_Shape.F90` ice-shape descriptors) raises `NotImplementedError` unless `allow_shed_placeholder=True` (same placeholder-guard idiom as `core/lookup_tables.py`'s breakup-fragment tables). See "Known divergences" for the `flg1` per-box selector finding. |
| `core/vapor_deposition.py` (`vapor_deposition_liquid`) | `vapor_deposition`, LIQUID-reachable subset of every quoted routine (init + `d_mean_mass`/shifted-boundary/evaporation-check block, `mod_amps_core.F90` lines 484-614) + Chen & Lamb (1994) semidiscrete growth helpers + `cal_lincubprms_vec` linear/cubic mass-space bin remap (`mod_amps_utility.F90:9798-10031`, full linear-fit / truncated-support / cubic-upgrade cascade) + aerosol-evaporation gather and `add_tendency_ap_vec` (`mod_amps_core.F90:21606-21665`) | G3 §1/§2/§4; `m2a-task-5-report.md` | `vapor_deposition`'s own quoted body lives inside `if(g%token==2)` (ICE) in G3's extraction, but every helper it calls is phase-generic and independently reachable for liquid (confirmed against `diag_pq`'s phase==1 `coef(1)/coef(2)`, Task 2) — this module ports the liquid-reachable subset of each, not a reinterpretation of an ice-only routine. `s_v_n` (in-substep supersaturation) is RECOMPUTED from the post-activation `ThermoState` via the same formula `core.activation`'s own `_liquid_supersaturation` uses, proven algebraically identical to the Fortran's persistent `ag%TV(n)%s_v_n(1)` by construction. Ice-only habit/growth-mode work (`assign_Qp_v3_vec`, axis growth, `dep_mass3_vec2`'s ice-mass partition) is M3 scope, not ported. |
| `core/repair.py` (`repair_liquid`, `repair_vapor`) | `repair`'s two independent phases (`mod_amps_check.F90:22-1181`): af_col = `cal_mass_budget_col`/`cal_con_budget_col`/`cal_vol_budget_col` (`mod_amps_check.F90:1261-1466` for the mass leg); af_vap = `cal_mass_budget_vapor` (`mod_amps_check.F90:2034-3145`) | G5 §4 (outer driver loop only — both routine bodies read directly, dispatch-authorized); `m2a-task-6-report.md` | `repair_liquid` (af_col, `phase="collision"`) and `repair_vapor` (af_vap, `phase="vapor"`) are DIFFERENT algorithms, not a shared implementation — an earlier draft incorrectly called `repair_liquid` for both (caught in code review). `cal_vol_budget_col`'s leg is a documented no-op here (keyed on ice-only `Vcs`; `LiquidPPV` has no volume field — liquid drops are diagnostically spherical). See "Known divergences" for the state-clip-vs-per-tendency-ledger equivalence. |
| `driver/box.py` (`run_box`, `BoxResult`) | Orchestration bridge, no single Fortran routine | `m2a-task-7-report.md` | Bridges a `BoxCase`'s AMPS-packed state (+ `core.packing.moistthermo_mask`-derived `thil`/`qtp`) into `implementations.warm_loop.run_warm_micro_tendency`, looped `case.n_steps` times. Raises `NotImplementedError` for any ice-bearing case (`moistthermo_mask`'s `qi>0`, M3 scope). `final_thermo`'s `thv`/`piv`/`moist_denv` come back bit-identical to the input (the same `update_airgroup` gap `warm_loop.py` already documents) — only `tv`/`qvv`/`liquid`/`aerosol` are genuinely advanced; per-call comparisons against a dumped "post" `MicroRecord` must restrict to those fields. |
| `driver/ref_data.py` (replay usage) | Unchanged from M1 (`MicroRecord`/`SedRecord` typed dump reader) | `m2a-task-7-report.md` (consumer, not a new port) | Newly exercised by M2a's per-call replay path: `driver/box.py`'s `case_from_micro_record` turns one phase="pre" `MicroRecord` into a `BoxCase`; `run_box(case_from_micro_record(pre))` is meant to be diffed against the SAME pair's phase="post" record (`RefDataset.micro_pairs()`). The harness (`tests/amps/integration_tests/test_warm_replay.py`) is implemented and wired but SKIPPED in every environment without local `.bin` dumps — replay validation is PENDING real reference data, not a code gap. `case_from_micro_record` is also `ThermoState`'s second producer (alongside `core.packing._pack_thermo`) for the CGS-canonicalization contract, see "Known divergences". |

## Not ported yet (pre-M2a baseline; see the M2a table above and the list below for the current, warm-phase-adjusted picture)

All physics process routines in `contrib/AMPS/mod_amps_core.F90` were, as of the M1 baseline, unported:

- Activation (`cal_aptact_var8_kc04dep` and friends) — **DONE for the warm/liquid `act_type=1` (KC04) path, M2a Tasks 3-4** (`core/activation.py`); the `act_type=2` `cal_aptact_var8_vec` twin remains unported (not selected by cloudlab).
- Vapor deposition — **DONE for the liquid-reachable subset, M2a Task 5** (`core/vapor_deposition.py`); ice-only habit/growth-mode work remains unported (M3).
- Coalescence (`cal_Coalescence_Efficiency` — only the liquid-liquid branch is *read*, for the breakup call-tree inventory below; not implemented)
- Breakup (`cal_breakup_dis_LL`, `cal_sig_sf`, `cal_Hmusig`, `zbrent`, `getznorm2` — full call-tree inventoried in `m1-task-5-report.md` Addendum 1, ~1285 lines across 6 files; BLOCKED as a physics-port task, not a LUT-conversion task)
- Melting
- Nucleation (`Ice_Nucleation1`, homogeneous/immersion freezing dispatch)
- Habit (habit-frequency table consumers)
- `diag_pq` — **liquid/`phase==1` branch DONE, M2a Task 2** (`core/liquid_diag.py`); the ice branch remains unported (M3).
- Repair — **DONE for both phases (af_col/af_vap), warm/liquid scope, M2a Task 6** (`core/repair.py`).
- Sedimentation (physics; `driver/ref_data.py` reads the *dumped* sed records but does not replay the sedimentation math)
- The real `cal_breakfragment` fill: `bu_fd`/`bu_tmass` are zero-initialized placeholders in `core/lookup_tables.py`, guarded against silent use (`NotImplementedError` unless `allow_placeholder=True`). Cloudlab has rain collisional breakup ON (`micexfg[18]`) — M2b needs the real values. Call-tree inventory (routine, file:lines, why) is in `m1-task-5-report.md` for the follow-up task to start from.

### M2a warm-phase: not-yet-ported (deferred to M2b/M2c/M3)

The warm-phase (liquid activation + condensation/evaporation + liquid
diagnosis) subset `cal_micro_tendency`/`ifc_cloud_micro` calls is complete
as of M2a; the following are the explicit residual items `implementations/
warm_loop.py`'s own module docstring identifies as OUT of this milestone's
scope:

- Coalescence / aggregation / riming (`coalescence(rain,rain)`, ice-ice aggregation, ice-rain riming) — **M2b**.
- Collisional + hydrodynamic breakup, including the real `cal_breakfragment` fill (`bu_fd`/`bu_tmass`) — **M2b**.
- All ice processes: ice nucleation (`Ice_Nucleation1`/`Ice_Nucleation2`), habit prediction, ice vapor deposition/growth, melting/shedding, `mv_ice2liq` — **M3**.
- Sedimentation, both the Euler and PPM (piecewise-parabolic) transport schemes — **M2c**.
- `diag_pq`'s ice branch (`phase==2`) — **M3**.

## Known divergences (intentional, documented)

| Area | Fortran | Python (M1) | Why / status |
|---|---|---|---|
| RNG (habit selection) | Park-Miller-style LCG | Counter-based hash: affine combine of (cell, level, bin, step) → 1 quadratic mixing round → 3 Park-Miller Lehmer rounds, mod `M31=2**31-1` | Bit-for-bit match with the Fortran LCG is impossible by construction (different algorithm family); statistically equivalent (mean~0.5, var~1/12, lag-1 corr <0.01, flat 16-bin histogram — validated in `spike_e_counter_rng.py`). Verification report (`docs/superpowers/facts/verification/rngv_verdict.md`) finds this irrelevant for cloudlab. |
| `l_gaxis_version` | 3 methods: v1 ("ORIGINAL"), v2, v3 ("METHOD 1"/"METHOD 2") | Only v1 implemented; v2/v3 raise `NotImplementedError` in both `pack_scale_to_amps` and `unpack_amps_to_scale` | M5 stub — F4 SS1.3/SS2.4 only summarize v2/v3 tersely; cloudlab config uses v1. |
| Reference data format | Raw binary streams (`AMPS_DUMP_micro`/`AMPS_DUMP_sed`, per-rank `.bin` files) | Binary streams read directly (`driver/ref_data.py`), or pre-aggregated into `.npz` via `scale-amps/scripts/amps_dump_reader.py` | No NetCDF; the Fortran-side dump format is intentionally kept as flat unformatted Fortran writes for cluster-side simplicity, converted off-cluster. |
| `esat` (saturation vapor pressure) | Table-lookup semantics (`QSPARM2` + truncate-then-clamp accessor) is what production Fortran actually calls | `esat_lk` (table, default used elsewhere) + `esat_analytic` (Lowe & Ficke, available but NOT the default path) | Both ported; table-lookup semantics preserved bit-exact (index arithmetic, M0 carry-forward #4) since that's what the Fortran call sites use. |
| Breakup fragment tables | `cal_breakfragment` computes real `bu_fd`/`bu_tmass` per bin pair | Zero-filled placeholders, hard-guarded (`NotImplementedError` unless `allow_placeholder=True`, plus runtime `is_placeholder` flag) | Physics fill deferred to M2 (see "Not ported yet"); guard prevents silent consumption of zeros as real data. |
| Osmotic-coefficient x-grid (`osm_ammsul`) | Non-uniform grid (0.1 step 0.0–1.0, 0.2 step 1.0–2.0, 0.5 step 2.0–5.5) | Initially inferred as uniform `np.linspace` (wrong); corrected to the verbatim non-uniform grid after reading `mod_amps_utility.F90:12996` directly | Resolved in Task 5 Addendum 1 — flagged here since the original submission's guess was wrong and worth remembering as a lesson (fact-file paraphrase ≠ verbatim quote). |
| KC04 deposition-freezing `flg1` selector (`cal_aptact_var8_kc04dep`'s `func_icevap_vec`, M2a) | `flg1=x(n)` assigned OUTSIDE the `do m=1,Lbx` box loop it later gates (`mod_amps_core.F90:8919-8925`), so `n` is whatever index was left over from a PRECEDING loop — every box's classical-nucleation (`iflg_dep==1`) gate ends up keyed off ONE arbitrary, stale box's supersaturation, not its own | `flg1 = x_arr` (per-box array) in `core/activation.py::func_icevap_vec` | Reads as an accidental Fortran scalar-aliasing artifact (leftover loop variable reused post-loop), not intended physics — no plausible reason box A's gate should depend on box B's `x`. Ported the evident per-box intent instead, per this project's divergence policy (port intent, document divergence, flag upstream). **Upstream-bug candidate — not verified against a scale_amps maintainer; worth confirming before relying on either behavior.** |
| `repair`'s state-clip vs. per-process-tendency ledger (`core/repair.py`, M2a Task 6) | `cal_mass_budget_col`/`cal_con_budget_col`/`cal_mass_budget_vapor` rescale a PER-PROCESS tendency ledger (`dmassdt(rmt,1:n_tendpros)`, ~12 named processes) via a multiplicative `modc_r`/`modc_v`, applied BEFORE `update_group_all` sums tendencies into `g%MS%mass`; af_col's `0.99999` tolerance band leaves marginally-negative (`<~1e-5` relative) bins uncorrected by design, and its zero-sum processes (e.g. collision-coalescence `j in {2,5,6,9}`) redistribute ALL bins by ONE bin's own `modc_r`, approximately preserving column totals | `repair_liquid`/`repair_vapor` operate directly on the ALREADY-ADVANCED `LiquidState`/`ThermoState` (no per-process ledger exists — each process hook returns its own advanced state, `update_group_all` inlined-by-design per `implementations/warm_loop.py`'s own docstring) — reduces algebraically to a single-bin/single-point `max(v, 0)` clip (af_col mass/con legs) and a point-level vapor floor + proportional-by-mass-share liquid giveback (af_vap) | Provably equivalent to Fortran's own NET effect on one bin/point in isolation (derivation in `core/repair.py`'s own module docstring), but loses two things Fortran's ledger-based algorithm has: (1) the `0.99999` marginal-negative tolerance band is structurally unreachable here (this port clips SOME bins Fortran would leave marginally negative — one-directional divergence), and (2) cross-bin/cross-process conservation for zero-sum processes has no analogue (this port touches ONLY the negative bin/point, never redistributes to "pay for" the clip). Expected, documented per-call divergence for any bin where multiple processes interact within one substep — not a rare corner case. |
| `ThermoState` CGS-canonicalization (`state.py`, M2a Task 7 hardening) | No single Fortran counterpart — a port-internal unit-contract fix. Prior to hardening, `ThermoState` stored `ptotv`/`moist_denv` in SI at its two producers (`core.packing._pack_thermo`, `driver.box.case_from_micro_record`), and every CGS-physics consumer (`core/thermo.py`, `core/liquid_diag.py`, `core/activation.py`, `core/vapor_deposition.py`, `implementations/warm_loop.py`) was expected to convert at its own point of use | `ptotv`/`moist_denv` are now canonicalized to CGS (`*10.0` Pa→dyn/cm², `*1.0e-3` kg/m³→g/cm³) ONCE, at the two producers; every consumer reads them AS-IS, no conversion at the point of use (`tv`/`thv`/`thetav` stay K — no base-unit change; `qvv` stays dimensionless) | Real bug class, caught TWICE by code review during Task 7 (`ptotv` first, then `moist_denv`) — masked in every earlier unit test because those fixtures happened to store the CGS-scaled number directly in the nominally-SI field (a spuriously-exact ratio of 1 hid the missing conversion). Converting once at the boundary shrinks the unit-discipline surface from an open-ended consumer set to two well-tested producers; see `state.py`'s own "UNIT CONTRACT" docstring on `ThermoState` for the full per-field disposition. Not a Fortran-vs-Python physics divergence — an internal contract that is now enforced structurally rather than by convention. |

## Carry-forward items referenced above

- **#1** (this task): `AMPS_DUMP_micro`/`AMPS_DUMP_sed` `dt` dummy kind `real(RP)`→`real(DP)` — fixed in `scalelib/src/atmosphere/physics/microphysics/scale_atmos_phy_mp_amps.F90`.
- **#2**: dump-reader version-mismatch checks are `raise ValueError`, not `assert` (Python `-O` safe) — done in `driver/ref_data.py` (M1 Task 8).
- **#4**: esat table accessor's truncate-then-clamp index arithmetic preserved bit-exact — done in `core/thermo.py` (M1 Task 2).

## Open leads (tracked; from real-dump validation)

- **Condensation magnitude under-production** (found in M2a real-data smoke vs `warm` dumps): the warm condensation path (activation + vapor_deposition) shows ~0.62 correlation / ~82% sign agreement with the Fortran bulk liquid-mass/vapor delta but explains only ~5% of magnitude (~6.8% on port-active levels, ~23% on port-silent levels). Most of the residual is the missing collision+breakup (per-bin spectral correlation ~0 = collision fingerprint) — so this lead can only be isolated AFTER M2b collision lands. Reproduction case: `warm` run, rank 0, TIME_AMPS=1800, i=j=3. Candidate hypotheses to check once collision is in and magnitude still under-produces: (a) `s_v_n` supersaturation precision in weakly-subsaturated regimes; (b) a units/scale issue in the vapor_deposition shift-bin remap for small `d_mean_mass`. Full analysis: icon4py `.superpowers/sdd/m2a-realdata-report.md` (gitignored).
- **allow_dep_placeholder run-level assumption**: the real-data replay sets `allow_dep_placeholder=True` unconditionally, valid for the `warm` run (documented no dust/IN aerosol) but NOT verified per-column against dumped aerosol category-2; must be revisited before pointing the harness at a seeding (ice) run.
